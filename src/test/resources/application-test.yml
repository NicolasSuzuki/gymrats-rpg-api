spring:
  datasource:
    url: jdbc:h2:mem:gymrats;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
app:
  cors:
    allowed-origin: http://localhost:3000
  security:
    jwt:
      secret: dGVzdC1zZWNyZXQtbXVzdC1iZS1hdC1sZWFzdC0zMi1ieXRlcw==
      expiration-seconds: 3600
